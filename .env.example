# Copy this file to ".env" (do NOT commit) to enable the LLM reviewer panel.
# Either or both keys can be set; reviewers run on whichever providers are available.

# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
